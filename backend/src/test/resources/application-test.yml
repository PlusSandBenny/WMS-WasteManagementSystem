spring:
  application:
    name: "Waste Management Backend (test)"
  datasource:
    url: "jdbc:h2:mem:wms;DB_CLOSE_DELAY=-1;MODE=MySQL"
    username: "sa"
    password: ""
    driver-class-name: "org.h2.Driver"
  jpa:
    hibernate:
      ddl-auto: "create-drop"
    show-sql: false
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.H2Dialect"

jwt:
  secret: "test-secret-change-me-please-test-secret-change-me-please-32b"

app:
  otp:
    return-code: true

